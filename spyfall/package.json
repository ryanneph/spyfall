{
  "name": "meteor-spyfall",
  "private": true,
  "scripts": {
    "start": "meteor --settings settings-dev.json run",
    "production": "meteor --settings settings.json run"
  },
  "dependencies": {
    "babel-runtime": "^6.26.0",
    "meteor-node-stubs": "~0.2.4"
  }
}
